rtimoshenko/docker-garbdi:


  Build:
    - CMD="ls -al"
    - echo $CMD && eval $CMD

  Start:
    - CMD="source /etc/environment"
    - echo $CMD && eval $CMD
    - export DB_HOST
    - export INFRA_ENV
    - export SERVER_TAGS
    - export INFRA_CONTAINER_DIR
    - CMD="ls -al"
    - echo $CMD && eval $CMD
